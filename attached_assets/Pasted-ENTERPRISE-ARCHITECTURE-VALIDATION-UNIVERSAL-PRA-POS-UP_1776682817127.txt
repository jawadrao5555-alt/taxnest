ENTERPRISE ARCHITECTURE VALIDATION — UNIVERSAL PRA POS (UPDATED)

IMPORTANT:
DO NOT WRITE CODE
DO NOT MODIFY FILES
ONLY ANALYZE AND CONFIRM

GOAL:
Design a UNIVERSAL PRA POS system with CATEGORY + USER-CONTROLLED FEATURES

CONTEXT:

We are NOT merging FBR POS (keep it separate)
We are ONLY redesigning PRA POS

CURRENT PROBLEM:
- Multiple POS views (simple + restaurant)
- Duplicate keyboard logic
- Bugs due to multiple implementations
- Inconsistent UI/UX

TARGET SYSTEM:

1. SINGLE UNIVERSAL PRA POS
- One POS screen for all businesses
- No separate POS views

2. CATEGORY = DEFAULT TEMPLATE
- On signup, user selects category:
  (restaurant, retail, pharmacy, etc.)
- System auto-applies default features

EXAMPLE:
Restaurant:
  kot = true
  tables = true
  kitchen = true
  inventory = true

Retail:
  barcode = true
  inventory = true

3. USER SETTINGS = FINAL CONTROL
- User can ENABLE/DISABLE ANY feature manually
- Category does NOT restrict user

Example:
Restaurant user can:
  disable KOT
  disable tables
  keep only simple billing

4. FEATURE FLAGS SYSTEM (CRITICAL)

Use companies.feature_flags (JSON)

Example:
{
  "kot": true,
  "tables": false,
  "inventory": true,
  "delivery": false
}

5. UI STRATEGY

- Use RESTAURANT POS UI as MASTER UI
- Same UI for ALL users
- Hide/show features using conditions

Example:
@if($features->kot)
   show KOT
@endif

NO separate views allowed

6. KEYBOARD SYSTEM

- Single keyboard controller (handleKey)
- No duplicate listeners
- Stable navigation
- Works for all modes

7. QUANTITY SYSTEM

- Controlled input (x-model.number)
- No buffer hacks
- No append bug
- Stable typing

8. DASHBOARD

- Single premium dashboard (restaurant-style)
- Dynamic widgets based on features

9. AGENT SYSTEM (CRITICAL)

- MUST remain unchanged
- Same endpoints
- Same payload
- Works for all categories

10. DATABASE

- Keep existing structure
- Add feature_flags column if needed
- No destructive changes

TASK:

1. CONFIRM:
Is this architecture clean, scalable, and production-safe?

2. IDENTIFY:
- Any flaw in category vs feature model?
- Any edge case missed?

3. VALIDATE:
- Can we safely use ONE UI for all businesses?
- Will conditional rendering scale well?

4. RISK ANALYSIS:
- What could break?
- Where bugs may appear?

5. IMPROVEMENTS:
- Any better approach than this?

6. MIGRATION STRATEGY:
- Best safe rollout approach (no downtime)

OUTPUT:
- Feasibility (YES/NO)
- Improvements (if any)
- Risks
- Final recommendation

DO NOT WRITE CODE
WAIT FOR APPROVAL