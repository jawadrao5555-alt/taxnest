ENTERPRISE UNIVERSAL PRA POS — FULL IMPLEMENTATION (CONTROLLED)

IMPORTANT:
EXECUTE IN PHASES INTERNALLY
BUT RUN IN SINGLE FLOW

DO NOT BREAK EXISTING SYSTEM
DO NOT MODIFY PRA / AGENT LOGIC
DO NOT REMOVE OLD POS UNTIL FINAL STEP

-------------------------------------

GOAL:
Build UNIVERSAL PRA POS using existing system

-------------------------------------

PHASE 1 — STABILIZE (MANDATORY FIRST)

1. Fix keyboard system in:
   resources/views/pos/create-invoice.blade.php

- Ensure ONLY ONE:
  @keydown.window="handleGlobalKey($event)"

- Single handler:
  function handleGlobalKey(e)

- Remove ALL duplicate listeners

2. Fix quantity input:

- Use clean logic:
  clear on focus
  commit on blur
  NO append
  NO buffer

-------------------------------------

PHASE 2 — FEATURE SYSTEM

1. Add column:
companies.feature_flags (json)

2. Create:
App\Services\PosFeatureService

- Return feature flags with defaults
- Based on business_category
- Allow overrides

-------------------------------------

PHASE 3 — COMPONENT EXTRACTION

From:
pos/restaurant/pos.blade.php

Extract components:

- cart
- product grid
- search bar
- payment section

Create:
resources/views/components/pos/

-------------------------------------

PHASE 4 — UNIVERSAL VIEW

Create:
resources/views/pos/universal.blade.php

- Use restaurant UI as base
- Wrap features:

@if($features->kot)
@if($features->inventory)

- Single keyboard system

-------------------------------------

PHASE 5 — CONTROLLER UPDATE

Update:
PosController::createInvoice()

- Add:
$features = PosFeatureService::forCompany($company)

- Return universal view ONLY if flag enabled

-------------------------------------

PHASE 6 — SAFE ROUTE

Add new route:

/pos/v2/invoice/create

- Use universal POS

DO NOT replace old route yet

-------------------------------------

PHASE 7 — SETTINGS UI

Create settings page:

- Toggle features:
  kot
  inventory
  tables
  delivery

-------------------------------------

PHASE 8 — TEST MODE

- Both POS run side by side:
  old + universal

-------------------------------------

PHASE 9 — FINAL SWITCH (ONLY AFTER CONFIRMATION)

- Replace:
/pos/invoice/create → universal

-------------------------------------

RULES:

- NEVER break existing POS
- NEVER change PRA logic
- NEVER change agent logic
- NO database destructive changes

-------------------------------------

OUTPUT:

1. What was implemented
2. Which files changed
3. Any risks
4. Ready for testing?

WAIT FOR APPROVAL BEFORE FINAL SWITCH