MASTER COMMAND — PHASE 5 → 8 (OBSERVABILITY + PERFORMANCE + SAFETY)

GOAL:
Run final production-readiness checks across:
- Phase 5 → Observability (logs, failures, retries)
- Phase 6 → Performance sanity
- Phase 7 → Security & data safety
- Phase 8 → Final production checklist

OUTPUT:
Single consolidated report (PASS/FAIL per phase)

────────────────────────────────────
CREATE FILE
scripts/phase_final_check.php
────────────────────────────────────

<?php

require DIR . '/../vendor/autoload.php';
$app = require_once DIR . '/../bootstrap/app.php';
$app->make(Illuminate\Contracts\Console\Kernel::class)->bootstrap();

use Illuminate\Support\Facades\DB;
use Illuminate\Support\Facades\Schema;

class FinalPhaseCheck {

    private $results = [];

    public function run() {
        $this->phase5_observability();
        $this->phase6_performance();
        $this->phase7_security();
        $this->phase8_final();

        $this->report();
    }

    // ─────────────────────────────
    // PHASE 5 — OBSERVABILITY
    // ─────────────────────────────
    private function phase5_observability() {
        $tests = [];

        // logs exist
        $logPath = storage_path('logs/laravel.log');
        $tests[] = $this->assert(file_exists($logPath), 'Log file exists');

        // retry columns
        $tests[] = $this->assert(Schema::hasColumn('invoices','retry_count'), 'Retry column exists');

        // failed invoices query
        $failed = DB::table('invoices')->where('fbr_status','failed')->count();
        $tests[] = $this->assert($failed >= 0, 'Failed invoices accessible');

        $this->results['PHASE 5 — OBSERVABILITY'] = $tests;
    }

    // ─────────────────────────────
    // PHASE 6 — PERFORMANCE
    // ─────────────────────────────
    private function phase6_performance() {
        $tests = [];

        $start = microtime(true);
        DB::table('invoices')->limit(10)->get();
        $time = microtime(true) - $start;

        $tests[] = $this->assert($time < 1, 'DB query under 1s');

        $this->results['PHASE 6 — PERFORMANCE'] = $tests;
    }

    // ─────────────────────────────
    // PHASE 7 — SECURITY
    // ─────────────────────────────
    private function phase7_security() {
        $tests = [];

        // important guards
        $tests[] = $this->assert(config('auth.guards.pos'), 'POS guard ok');
        $tests[] = $this->assert(config('auth.guards.fbrpos'), 'FBR guard ok');

        // env safety
        $tests[] = $this->assert(env('APP_ENV') !== 'local', 'Not running in local');

        $this->results['PHASE 7 — SECURITY'] = $tests;
    }

    // ─────────────────────────────
    // PHASE 8 — FINAL
    // ─────────────────────────────
    private function phase8_final() {
        $tests = [];

        // core tables exist
        $tables = ['invoices','pos_transactions','companies','users'];
        foreach ($tables as $t) {
            $tests[] = $this->assert(Schema::hasTable($t), "Table $t exists");
        }

        // no null totals
        $nulls = DB::table('invoices')->whereNull('total_amount')->count();
        $tests[] = $this->assert($nulls === 0, 'No null invoice totals');

        $this->results['PHASE 8 — FINAL'] = $tests;
    }

    private function assert($cond, $msg) {
        return [
            'status' => $cond ? 'PASS' : 'FAIL',
            'message' => $msg
        ];
    }

    private function report() {
        echo "\n===== FINAL PHASE REPORT =====\n";

        foreach ($this->results as $phase => $tests) {
            echo "\n$phase\n";
            foreach ($tests as $t) {
                echo ($t['status']=='PASS'?'✓':'✗') . " " . $t['message'] . "\n";
            }
        }

        echo "\n=============================\n";
    }
}

(new FinalPhaseCheck())->run();

────────────────────────────────────
RUN COMMAND
────────────────────────────────────

php scripts/phase_final_check.php

────────────────────────────────────
EXPECTED RESULT
────────────────────────────────────

✓ PHASE 5 — OBSERVABILITY
✓ PHASE 6 — PERFORMANCE
✓ PHASE 7 — SECURITY
✓ PHASE 8 — FINAL

ALL PASS → 100% READY FOR DEPLOY
ANY FAIL → FIX BEFORE GO-LIVE

IMPORTANT:
This is FINAL SAFETY GATE before production